nguage: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "3.5"
#  - "3.5-dev" # 3.5 development branch
#  - "nightly" # currently points to 3.6-dev
# command to install dependencies
install:
  - "pip3 install --user nose"
  - "cd src/cobwebs; pip3 install --user -r requirements.txt; pip3 install .; cd -"
  - "cd src/dbdriver; pip3 install --user -r requirements.txt; pip3 install .; cd -"
  - "cd src/apiviewer; pip3 install --user -r requirements.txt; pip3 install .; cd -"
  - "cd src/spider; pip3 install --user -r requirements.txt; pip3 install .; cd -"
# command to run tests
script: nosetests
